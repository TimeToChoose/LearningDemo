<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shineofeidos.demo.mapper.RefreshTokenMapper">

    <resultMap id="RefreshTokenMap" type="com.shineofeidos.demo.model.RefreshTokenRecord">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="revoked" column="revoked"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_token (user_id, token_hash, expires_at, revoked)
        VALUES (#{userId}, #{tokenHash}, #{expiresAt}, #{revoked})
    </insert>

    <select id="findValidByHash" resultMap="RefreshTokenMap">
        SELECT id, user_id, token_hash, expires_at, revoked, created_at
        FROM refresh_token
        WHERE token_hash = #{tokenHash}
          AND revoked = 0
          AND expires_at > NOW()
        LIMIT 1
    </select>

    <update id="revokeByHash">
        UPDATE refresh_token SET revoked = 1 WHERE token_hash = #{tokenHash}
    </update>

    <update id="revokeAllByUserId">
        UPDATE refresh_token SET revoked = 1 WHERE user_id = #{userId} AND revoked = 0
    </update>

</mapper>
